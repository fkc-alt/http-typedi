# This workflow will run tests using node and then publish a package to GitHub Packages when a release is created
# For more information see: https://docs.github.com/en/actions/publishing-packages/publishing-nodejs-packages
# npm ci === npm install

name: http-typedi

on:
  pull_request:
    branches:
    - main
  push:
    branches:
    - main
jobs:
  publish-npm:
    runs-on: ubuntu-latest
    env:
      PACKAGE_VERSION: ${{ steps.get_package_version.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Get Package Version
        id: get_package_version
        run: echo "::set-output name=version::$(node -p "require('../../package.json').version")"
      - name: Publish
        run: |
          if [ "$PACKAGE_VERSION" != "${{ steps.get_package_version.outputs.version }}" ]; then
            echo "Package version has changed. Publishing new version to NPM..."
            npm publish
          else
            echo "Package version has not changed. Skipping NPM publish."
          fi

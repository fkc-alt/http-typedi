import { defineConfig } from 'vitepress'

export default defineConfig({
    // site-level options
    title: 'VitePress',
    description: 'Just playing around.',
    base: './',
    themeConfig: {
        // theme-level options
    }
})